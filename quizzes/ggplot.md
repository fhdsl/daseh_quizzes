{quiz, id: Esquisse / ggplot, attempts: 10}

## ggplot


{choose-answers: 4}
? What is the basic layer in any plot created using ggplot2?

m) `geom_line()`
m) `theme()`
C) `ggplot()`
m) `geom_point()`

{choose-answers: 4}
? Which function do you use if you want to fill a boxplot or a bar?

C) `fill()`
m) `color()`
m) `geom_fill()`
m) `aes()`

{choose-answers: 4}
? What does the function `aes(x=..., y=...)` do?

m) It specifies the number of subplots the plot will have.
m) It specifies the size of the plot.
m) It specifies the coordinates to be used in the plot.
C) It specifies which variable will correspond to which aspect of the plot.

{choose-answers: 4}
? Which function can be used to split a complicated plot into several subplots?

o) `geom_line()`
C) `facet_grid()`
m) `aes(x=...,y=...)`
m) `geom_plot()`
o) `subplots()`

{choose-answers: 4}
? How do you combine different layers of a plot?

C) By adding a `+` at the END of a line.
o) By adding a `%>%` at the END of a line.
m) By adding a `+` at the BEGINNING of a line.
0) By adding all the layers in a vector separated by commas (e.g c(layer1, layer2, layer3)).
m) y adding all the layers in a parenthesis separated by commas (e.g. (layer1, layer2, layer3)).

{/quiz}